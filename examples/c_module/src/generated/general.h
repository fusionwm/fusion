// Generated by `wit-bindgen` 0.51.0. DO NOT EDIT!
#ifndef __BINDINGS_GENERAL_H
#define __BINDINGS_GENERAL_H
#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

typedef struct general_string_t {
  uint8_t*ptr;
  size_t len;
} general_string_t;

// Imported Functions from `plugin:general/logging`
extern void plugin_general_logging_debug(general_string_t *message);
extern void plugin_general_logging_info(general_string_t *message);
extern void plugin_general_logging_warn(general_string_t *message);
extern void plugin_general_logging_error(general_string_t *message);

// Imported Functions from `plugin:general/config`
extern void plugin_general_config_config_get(general_string_t *path, general_string_t *ret);
extern void plugin_general_config_config_delete(general_string_t *path);

// Exported Functions from `general`
void exports_general_init(void);

// Helper Functions

// Sets the string `ret` to reference the input string `s` without copying it
void general_string_set(general_string_t *ret, const char*s);

// Creates a copy of the input nul-terminated string `s` and
// stores it into the component model string `ret`.
void general_string_dup(general_string_t *ret, const char*s);

// Creates a copy of the input string `s` with length `len` and
// stores it into the component model string `ret`.
// The length is specified in code units (bytes for UTF-8, 16-bit values for UTF-16).
void general_string_dup_n(general_string_t *ret, const char*s, size_t len);

// Deallocates the string pointed to by `ret`, deallocating
// the memory behind the string.
void general_string_free(general_string_t *ret);

#ifdef __cplusplus
}
#endif
#endif
